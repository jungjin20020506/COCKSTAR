// ===================================================================================
// 온보딩 시청 기록 키
// -----------------------------------------------------------------------------------
// 컴포넌트 파일이 아니라 여기 따로 둔 이유는 두 가지다.
//  ① Fast Refresh는 컴포넌트만 내보내는 파일에서 제대로 동작한다.
//     같은 파일에서 상수를 함께 내보내면 편집할 때마다 화면 상태가 통째로 날아간다.
//  ② 이 키를 읽는 쪽이 안내 컴포넌트 전체를 불러올 필요가 없어진다.
//
// ★ 전원에게 안내를 다시 띄우려면 여기 버전을 올린다 ('-v1' → '-v2').
//   저장된 데이터를 지우는 방식은 로컬 기록이 남은 기기에서 안 떠서 새는 사람이 생긴다.
// ===================================================================================

/** 처음 가입한 사람에게 보여주는 앱 소개 (스킵 불가) */
export const WELCOME_TOUR_KEY = 'cockstar-welcome-v1';

/** 어떤 방이든 '관리자로서 처음' 들어왔을 때 보여주는 운영 안내
 *  v2: 공지사항·타임라인·테마색·스와이프·3연속 방지 페이지 추가 (2026-08-24) —
 *  기존 관리자에게도 새 기능 페이지를 한 번 보여주려고 버전을 올렸다 */
export const ROOM_ADMIN_GUIDE_KEY = 'cockstar-room-admin-v2';

/** 경기방을 처음 만들 때 보여주는 항목 설명 */
export const CREATE_ROOM_GUIDE_KEY = 'cockstar-create-room-v1';

/** 자동매칭 게임형 안내 (기존) */
export const AUTOMATCH_GUIDE_KEY = 'cockstar-automatch-v1';

/** 모든 튜토리얼 키 — '안내 다시 보기'에서 한 번에 초기화할 때 쓴다 */
export const ALL_GUIDE_KEYS = [
    WELCOME_TOUR_KEY,
    ROOM_ADMIN_GUIDE_KEY,
    CREATE_ROOM_GUIDE_KEY,
    AUTOMATCH_GUIDE_KEY,
];

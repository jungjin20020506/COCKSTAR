# 2026-08-31 업데이트

여덟 가지 요청을 한 번에 반영했다. 핵심 줄기는 세 가지 — **공유 링크 404의 근본 수정**,
**휴식 상태의 자동 변경 전면 금지**, **QR 초대·알림·설치 유도 강화**.

---

## 1. 공유 링크·새로고침 404 (아이폰 포함) — 근본 원인 수정

**원인**: 앱은 Vercel(cockstar.vercel.app)에서 서비스 중인데 SPA rewrite 설정이 없었다.
`/room/abc` 같은 주소는 서버에 실제 파일이 없으므로 Vercel이 404를 돌려줬다.

- 안드로이드에서 덜 보였던 이유: PWA 서비스 워커가 설치된 기기는 `navigateFallback`이
  로컬에서 index.html을 돌려줘 서버까지 안 갔다.
- 아이폰에서 많았던 이유: 사파리 첫 방문·카톡 인앱 브라우저에는 서비스 워커가 없어
  매번 서버 404를 그대로 맞았다.

**수정**: `vercel.json` 추가 — 모든 경로를 `index.html`로 rewrite (정적 파일은 우선 서빙).
`sw.js`는 no-cache, `/assets/*`는 immutable 캐시 헤더도 함께 걸었다.
→ **다음 Vercel 배포부터 적용된다.** (main 푸시로 자동 배포되는 경우 이 커밋으로 끝)

## 2. 아이폰 '먹통(튕김)' 추가 수정 — Firestore 내부 오류 자동 복구

로컬 테스트 중 Firestore SDK의 `INTERNAL ASSERTION FAILED (ID: ca9)`를 실제로 재현했다.
IndexedDB 캐시가 꼬이면(특히 iOS 사파리) 실시간 구독이 통째로 죽어 화면이 멈춘다.

- firebase SDK 12.2 → **12.18** 업그레이드 (관련 수정 다수 포함)
- `src/firebase.js`에 감지 장치 추가: 이 오류가 잡히면 30초 쿨다운을 두고
  자동 새로고침 한 번으로 SDK를 다시 세운다 (무한 루프 방지 포함)

## 3. 휴식 기능 전면 개편 — "자동으로는 절대 안 바뀐다"

**버그 원인**: 45분간 하트비트가 없으면 자동으로 휴식 처리하는 코드가 있었다.
경기 중에는 제외되지만, 화면을 꺼둔 채 경기하던 사람은 **경기 종료 직후** 자동 휴식으로
바뀌었다 — "경기 끝나니 강제 휴식됐어요"의 정체.

바뀐 규칙:

| 규칙 | 내용 |
|---|---|
| 자동 휴식 **삭제** | 휴식 상태는 본인 버튼 또는 관리자 조작으로만 바뀐다 |
| 관리자 조작 | 선수 카드를 **길게 누르면** 선수 관리 창 — 휴식/복귀 토글 추가 |
| 휴식 ≠ 결격 | 휴식 중이어도 경기예정 배치·경기 시작이 그대로 된다 |
| 예약 유지 | 휴식을 켜도 이미 잡힌 예약 경기에서 빠지지 않는다 |
| 유일한 제외 | 새 **자동 매칭 후보**에서만 빠진다 (쉬고 싶다는 뜻이므로) |

- `repairMatchQueues`: 휴식은 더 이상 해체 사유가 아니다 — '나간 사람'만 정리
- 자리 비움 정리는 관리자가 설정에서 목록 보고 직접 내보내는 방식 그대로 유지

## 4. 튕김·앱 종료 시 선수 카드 유지 확인

코드 전수 확인 결과, 선수 문서를 지우는 경로는 ① 본인이 '나가기' 확인 ② 관리자 내보내기
③ 관리자 '자리 비움 정리' ④ 전원 내보내기 — 네 가지뿐이다. `beforeunload`류 자동 퇴장은
원래 없었고, 유일한 자동 변경(자동 휴식)도 이번에 제거했다. 튕김·새로고침·앱 종료로는
명단에서 절대 빠지지 않는다.

## 5. QR 초대 (상단바 실험실 → QR 버튼)

- 상단바의 실험실(플라스크) 버튼과 시뮬레이션 랩을 **완전히 삭제** (봇 투입·자동 재생 포함)
- 그 자리에 **QR 버튼** — 누구나 누르면 방 QR이 크게 뜬다. 카메라로 스캔하면
  `/room/:id`로 바로 입장 (1번의 rewrite 수정 덕에 스캔 입장도 404 없이 동작)
- 공유 모달에도 'QR 코드로 초대' 진입점 추가
- `qrcode` 라이브러리는 모달을 열 때만 동적 로드 (첫 화면 무게 0)

## 6. 자동매칭 대기석 이름 잘림

원인: 경기 번호(w-7)·시작 버튼(w-16)이 카드 폭을 갉아먹어 4열 카드가 대기 명단보다
20px쯤 좁았다. **번호·안내문·시작 버튼을 윗줄로 올리고 카드 4장이 전체 폭**을 쓰도록
재배치 — 대기 명단과 같은 폭이라 같은 이름이면 똑같이 보인다.

## 7. 알림 강화 (갤럭시·아이폰)

기존에도 '다음 경기는 나'(대기 1번)와 'N번 코트로 입장'(경기 시작)에 배너+진동+소리가
울렸다. 이번에 **권한 유도**를 강화:

- 방 참가 직후, 알림 가치가 가장 와닿는 순간에 전용 모달로 한 번 권한다
  (기기당 한 번, '나중에'를 누르면 다시 안 물음)
- 아이폰 사파리(알림 API 없음)에서는 같은 모달이 "홈 화면에 추가하면 알림을 받을 수
  있어요"로 설치 안내에 연결 (iOS 16.4+ 설치형 PWA만 웹 알림 지원 — 애플 정책)
- 기존 MyTurnBanner의 인라인 유도도 그대로 유지

## 8. PWA 설치 유도 강화

- 설치 배너를 더 크고 빠르게(1.5초), 문구를 상황별로: 크롬/갤럭시에서 네이티브 설치창이
  가능하면 **"바로 설치"** — 버튼 한 번에 설치창이 뜬다
- 아이폰은 시스템상 버튼 설치가 불가(애플 정책) — 그림 단계 안내 유지, 알림 모달에서도 연결
- 카톡 인앱 브라우저는 설치 자체가 불가 — '기본 브라우저로 열기' 탈출 안내 유지
- 거부해도 모든 기능은 브라우저에서 그대로 사용 가능

---

## 7-1. (3차) "알림이 안 와요" 수정 — 권한 요청이 실제로 뜨게 + 상단 상태 배너

**원인**: 알림 권한 모달이 '참가 버튼을 누르는 순간'에만 떴다. 이미 명단에 있는
사용자(= 기존 사용자 전원)는 방에 들어와도 그 코드가 실행되지 않아 권한 요청을
영영 못 봤고, 권한이 없으니 시스템 알림도 못 갔다.

- **입장 시에도 요청**: 이미 참가된 채로 방에 들어와도 알림 권한 모달이 한 번 뜬다
  (안내 모달과 겹치지 않게 타이밍 조절, 기기당 한 번)
- **상단 고정 배너 신설**(NotiPermissionBanner): 권한이 허용되기 전까지 방 상단에
  계속 보인다 — "알림을 허용해야 내 차례·경기 시작 안내를 받을 수 있어요 [켜기]"
  - 켜기 → 그 자리에서 브라우저 권한 창 → 허용 시 즉시 테스트 알림 발송(작동 확인)
  - 거부됨 → 브라우저 설정에서 여는 길 안내
  - 카톡 인앱 → "브라우저로 열기" / 아이폰 사파리(비설치) → 홈 화면 추가 안내로 연결
  - 허용되면 배너는 사라진다. 설정에서 바꾸고 돌아와도 상태를 다시 읽는다
- MyTurnBanner 안의 일회성 인라인 안내는 제거 (배너와 중복)

## 8-1. (2차) 설치 유도 대폭 강화 — 원버튼 탈출 → 도착 즉시 설치

배너만으로는 약하다는 피드백에 따라 **하루 한 번 뜨는 설치 모달**(InstallNudgeModal)을
추가했다. 환경마다 '가장 짧은 설치 경로'를 큰 버튼 하나로 내민다.

| 환경 | 버튼 한 번에 일어나는 일 |
|---|---|
| 카톡·라인 인앱 (갤럭시/아이폰 공통) | **"브라우저로 열고 설치하기"** → 크롬/사파리로 탈출. 탈출 주소에 `install=1`이 붙어 **도착 즉시 설치 안내가 자동으로 이어진다** |
| 갤럭시 크롬 | **"지금 설치하기"** → 네이티브 설치창이 바로 뜬다 |
| 아이폰 사파리 | 그림 단계 안내(공유 → 홈 화면에 추가)가 바로 열린다 — 애플이 버튼 설치를 막아둔 환경에서의 최단 경로 |

직접 흐름을 돌려보며 잡은 이탈 지점:

- **경기방으로 바로 들어온 사람에게는 자동 모달을 안 띄운다** — 카톡 초대를 받고 온
  사람의 목적은 '참가'다. 참가 확인 → 알림 권한 위에 설치 모달까지 3겹이면 나간다.
  (단, '브라우저로 열고 설치하기'를 직접 누르고 온 `install=1` 도착은 설치가 목적이므로 예외)
- **탈출 스킴이 막힌 기기 대비** — 버튼이 조용히 실패해도 막다른 길이 안 되도록
  "버튼이 안 되면? ⋯ 메뉴로 여는 방법 보기" 보조 출구를 붙였다.
- **StrictMode 이중 실행 버그 선제 수정** — `install=1` 소비를 effect 가 아니라
  모듈 로드 시점 1회로 옮겼다 (effect 에서 읽으면 개발 모드에서 첫 실행이 파라미터를
  지워버려 모달이 안 뜬다).
- 닫기는 언제나 한 번, 운영일 기준 하루 1회만, 설치되면 영영 안 뜬다.
  안 깔아도 모든 기능 그대로.

## 검증

- `npm run verify` (lint 0 err · tsc · vitest 59개 · build) ✅
- `test:matching` 전 시나리오 ✅ · `test:matching:stress` 40개 시나리오 267건 ✅
- `test:integration` 42건 ✅ (휴식 정책 변경에 맞춰 5-2·5-3 기대값 갱신)
- `test:updates` ✅
- 신규 테스트 8건 추가 (`src/test/roomUpdates0831.test.jsx`) — QR 모달, 알림 권한 모달
  (지원/미지원), 관리자 휴식 토글, 휴식 선수 낀 경기 시작 가능, repair 정책
- 로컬 dev 서버에서 홈·스토어·로비·방 딥링크·비로그인 흐름 실제 렌더 확인

## 배포 후 확인 사항 (사람이 한 번)

1. Vercel 배포 완료 후 `https://cockstar.vercel.app/room/아무거나` 를 시크릿 창에서 열어
   404 대신 앱이 뜨는지 확인
2. 카톡으로 방 공유 → 아이폰에서 링크 접속 확인
3. QR 버튼 → 다른 폰 카메라로 스캔 → 입장 확인

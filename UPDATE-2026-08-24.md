# 2026-08-24 업데이트 정리 — 확인 체크리스트

각 항목 옆 ☐ 에 직접 확인하면서 체크하세요.
번호는 아이디어 100 목록의 번호입니다.

---

## 🏸 경기방

- ☐ **[6] 연속 경기 제한** — 매칭 엔진이 "쉼 없이 3연속 경기가 되는 선수"를 감점합니다.
  - 확인법: 한 선수를 쉬는 시간 없이 2경기 연속 뛰게 한 뒤 자동매칭을 누르면,
    그 선수가 포함된 후보에 `쉼 없이 3연속 경기가 되는 선수: ○○` 사유가 뜨고 순위가 내려갑니다.
  - 시뮬레이션 40개 시나리오 · 검증 267건 전부 통과 상태로 반영.
- ☐ **[8] 매칭 히스토리 타임라인** — 경기 진행 탭 맨 아래 `🕐 오늘의 매칭 타임라인` 버튼.
  끝난 경기를 시간순으로(시각 + 4명 이름) 보여줍니다. 관리자 복기용이지만 누구나 볼 수 있어요.
- ☐ **[25] 방 공지** — 방 정보 수정에 `📢 공지` 칸 추가. 저장하면 방 화면 상단에 고정되고,
  새 공지는 모든 참가자 화면에서 확성기가 흔들리며 반짝 등장합니다. 관리자는 공지를 눌러 바로 수정.
- ☐ **[25+] 방 입장 연출** — 방에 들어가면 라임 플래시 → 방 이름 슬램 → 셔틀콕이 날아가는
  약 2초 오프닝. 공지가 있으면 이어서 떠오릅니다. 아무 곳이나 탭하면 즉시 건너뛰고,
  세션당 방 하나에 한 번만 나옵니다. (기기 '모션 줄이기' 설정이면 아예 안 나옴)
- ☐ **[74] 빈 코트 펄스** — 올릴 수 있는 경기가 있는데 코트가 비어 있으면 코트 카드가
  라임색으로 숨쉬며 `다음 경기를 올려주세요`가 표시됩니다.
- ☐ **코트 입장 연출** — 경기를 시작하면 코트 카드에 선수 4장이 슬롯머신처럼 착착 꽂힙니다.
- ☐ **[79] 스와이프 탭 전환** — 방 화면을 좌우로 쓸면 매칭 대기 ↔ 경기 진행이 전환됩니다.
- ☐ **[80] 데스크톱 2열** — PC(가로 1024px 이상)에서 방을 열면 왼쪽 매칭 · 오른쪽 코트가
  나란히 펼쳐지고 탭이 사라집니다.
- ☐ **[78] 방 테마 색** — 방 정보 수정에서 포인트 색 6종(라임·스카이·코랄·바이올렛·골드·민트) 선택.
  방 헤더 스트립·탭·MATCH 배지·코트 헤더·로비 카드에 그 색이 입혀집니다.
- ☐ **선수 카드 ⋯ 버튼 제거** — 요청대로 뗐습니다. 경기 수 수정은 예전처럼 카드를
  **길게(0.7초) 누르면** 열립니다. (관리자 안내 튜토리얼에 이미 설명돼 있음)
- ☐ **[77] 색약 대응** — 선수 카드의 남/여 구분에 색 띠에 더해 ♂/♀ 기호가 함께 표시됩니다.
- ☐ **대기 시간 "약 N~N분"** — 내 차례 배너의 예상 대기가 한 경기 10분 기준,
  활성 코트 수로 나눠 범위로 표시됩니다. 예: 앞 3경기 · 2코트 → `약 10~20분`.
  바로 다음이면 `약 10분 이내`.

## 🔒 관리자 동시 조작 안전화

- ☐ **내보내기 중복 방지** — 관리자 둘이 같은 선수를 동시에 내보내도 인원 수가
  두 번 줄지 않습니다(트랜잭션으로 전환). 자리비움 일괄 정리도 같은 방식.
- ☐ **[90] 휴식 토글 즉시 반응** — 휴식/복귀 버튼이 서버 응답을 기다리지 않고 바로 바뀝니다.
  실패하면 자동으로 되돌아가요. (찜은 이미 적용돼 있었음)
- 참고: 경기 시작/종료/슬롯 배치/자동매칭 추가는 이전부터 전부 트랜잭션 + 충돌 안내였고 그대로 유지.

## 🔔 알림 (45 · 48 · 51)

- ☐ **[51] 알림 센터** — 홈의 종 아이콘이 이제 진짜로 동작합니다. 지난 알림 목록(50개),
  안 읽은 알림 빨간 점, 알림 켜기 버튼, 소리 스위치가 있습니다.
- ☐ **[45] 내 차례 알림** — 상태가 바뀌는 순간에 알립니다:
  `내 경기가 잡혔어요`(조용히·진동만) → `다음 경기는 나!`(배너+진동+소리) → `N번 코트로 입장하세요`.
  방 화면에 "알림 켜줄까요?" 한 줄 안내가 한 번 나오고, 닫으면 다시 안 나옵니다.
- ☐ **[48] 소리 "띠링"** — 파일 없이 WebAudio 로 만든 2음 차임. 알림 센터에서 on/off.
- ☐ **진동** — 안드로이드에서 상태별로 다른 패턴. (아이폰 웹은 진동 API 자체가 없어 불가)
- ⚠️ **한계(정직하게)**: 지금 알림은 **앱이 켜져 있는 동안**(백그라운드 탭·홈화면 PWA 포함) 옵니다.
  앱을 완전히 종료해도 오는 서버 푸시(FCM 발송)는 Cloud Functions 서버가 필요해서
  이번에 안 들어갔고, 진행 순서를 `docs/OPS.md` 2번에 정리해뒀습니다.
  아이폰은 iOS 16.4+ 에서 **홈 화면에 추가한 PWA**만 시스템 알림이 가능합니다(애플 정책).

## 📱 PWA 설치 유도

- ☐ **더 일찍, 더 넓게** — 설치 배너가 3번째 → 2번째 방문부터, 닫으면 2주 → 1주 조용히.
  beforeinstallprompt 가 없는 안드로이드 브라우저(삼성 인터넷 등)에도 수동 안내를 띄웁니다.
- ☐ **막힘 없음 보장** — 배너는 X 한 번으로 닫히고, 안 깔아도 모든 기능이 브라우저에서 동일하게
  동작합니다. 아이폰은 그림 3단계 안내(공유 → 홈 화면에 추가 → 추가).
- ☐ 알림 센터에서 아이폰 사용자가 "알림 켜기"를 누르면 설치 안내로 자연스럽게 이어집니다
  (아이폰은 설치해야 알림이 되므로 — 설치의 실질적 이유를 제공).

## 🛍️ 스토어

- ☐ **[61] 상품 자동 갱신** — GitHub Actions 가 매주 월요일 새벽 3시(KST)에 공식몰 상품을
  다시 받아 자동 커밋합니다. 실패하면 저장소에 이슈가 열리고 메일이 갑니다
  (이슈 본문에 jung22459369@gmail.com 문의 안내 포함). 수동 실행: Actions 탭 → Run workflow.
- ☐ **[65] 최근 본 상품** — 상품을 눌러 공식몰에 다녀오면 스토어 맨 위에
  `최근 본 상품` 가로줄이 생깁니다(기기 저장, 최근 12개).
- ☐ **[87] 이미지 최적화** — 공식몰 배너 829KB PNG → **98KB WebP** (동일 화질, 1/8 용량).

## 🗺️ 콕맵 · 로비

- ☐ **[75] 다크 지도** — 카카오맵에 어두운 필터가 입혀져 앱 톤과 맞습니다.
- ☐ **[72] 당겨서 새로고침** — 로비 목록을 아래로 당기면 스피너가 돌고, '가까운 순'이면
  내 위치를 다시 잽니다. (지도 화면은 팬 제스처와 겹쳐서 뺐습니다)
- ☐ **[73] 스켈레톤 → 콘텐츠 페이드** — 로비 카드가 로딩 후 아래에서 사르륵 올라오며 나타납니다.

## 🛡️ 운영 · 안전

- ☐ **[97] 신고 기능** — 방 화면 하단(비관리자)에 `신고` 버튼. 사유 선택 + 상세 입력.
  슈퍼 관리자는 내 정보 → `신고 검토`에서 대기열을 보고 처리 완료 표시.
  보안 규칙: 신고자는 본인 이름으로만 접수, 열람·처리는 슈퍼 관리자만.
- ☐ **[96] 버전 강제 업데이트 게이트** — Firestore `config/app` 문서에 `minVersion` 을 쓰면
  그보다 낮은 버전은 "새 버전 받기" 화면에 갇힙니다(캐시·서비스워커 청소 후 새로 받음).
  사용법은 `docs/OPS.md` 3번.
- ☐ **[99] 비용 알림** — 코드로는 불가능한 영역(콘솔 설정)이라 `docs/OPS.md` 1번에
  5분짜리 설정 절차를 적어뒀습니다. **이건 콘솔에서 직접 한 번 해주셔야 합니다.**

## 🔍 로그인 흐름 점검 결과 (직접 사용해봄)

브라우저로 실제 화면을 눌러가며 확인했습니다. 단, 계정 생성·비밀번호 입력 자체는
제 안전 규칙상 대신할 수 없어서 **인증번호 전송 직전 단계까지** 전부 검증했습니다.

- ✅ 로그인 모달: 카카오 / 휴대폰 / (관리자) 아이디 세 경로 모두 정상 진입
- ✅ 휴대폰 입력칸: `type=tel + inputMode=numeric + autocomplete=tel` — 모바일에서 숫자 키패드가
  바로 뜨고, 인증번호 칸은 `one-time-code` 라 문자 도착 시 자동완성이 뜹니다
- ✅ 빈 값 제출 시 에러 문구 정상 (`휴대폰 번호를 입력해주세요.`)
- ✅ 계정 찾기 화면: "카카오·휴대폰 가입은 비밀번호가 없다 → 같은 방법으로 재로그인" 안내가
  가장 위에 있어 헤매지 않음. 최후 수단으로 카톡 문의 + 이메일 노출
- ✅ 자동 포커스, 뒤로가기, 재시도(reCAPTCHA 재생성) 동작 확인
- 💡 개선 여지(다음에): 휴대폰 번호 입력 중 자동 하이픈(010-1234-5678) 표시 정도가 남은 마찰

## ✅ 자동 검증 통과 내역

- ESLint 0 errors · TypeScript 통과 · 단위 테스트 39개 통과
- 매칭 시뮬레이션 시나리오 통과 · 스트레스 40개 시나리오 검증 267건 통과
- 프로덕션 빌드 성공 (PWA 프리캐시 27개 파일)

## 🧪 업데이트 전용 시뮬레이션 (`npm run test:updates`) — 42건 전부 통과

이번 주 대규모 운영을 가정하고 오늘 바뀐 것만 집중적으로 돌렸다.

1. **연속 경기 제한 A/B** — 감점을 끄고/켠 채 같은 씨앗으로 4개 시나리오 × 3씨앗 비교.
   무휴식 3연속이 줄거나 같고(혼합 대형에서 98→65회), 편차·가동률은 나빠지지 않음.
   4~5명 극소 인원(연속이 물리적으로 불가피)에서도 오류 없이 완주.
2. **대규모 하루 풀 시뮬레이션** — 42명 · 6코트 · 5시간, 지각 4명 · 중도 이탈 2명 ·
   휴식 2명 섞어서 5개 씨앗. 전 씨앗에서 148~151경기 · 편차 ≤4 · 가동률 107%+ ·
   같은 4명 재탕 0 · 깨진 예약 자동 해체 정상 동작.
   - 발견한 설계 특성: S조·D조처럼 급수 양극단 소수는 5시간에 평균 대비 2~3경기
     적게 침(급수 보호와 공평의 균형점 — 엔진 구출선 -3 안쪽이라 정상).
3. **대기시간 "약 N~N분"** — 예약→실제 시작까지 217건 실측, **적중률 100%** (범위 ±3분).
4. **버전 게이트** — 8가지 비교 케이스 (두 자리 버전, 깨진 값 등) 전부 정상.
5. **알림 모듈 실제 실행** — 기록 50개 상한·안읽음 카운트·소리 설정·깨진 저장값 복구·
   미지원 브라우저 안전 종료 등 11건.
6. **알림 트리거 상태기계** — 9가지 상태 전환에서 울릴 때만 울림 (스팸 없음).
7. **관리자 2명 동시 조작** — 내보내기/일괄정리/경기시작/슬롯배치/경기종료/자동매칭추가
   6종 경합을 트랜잭션 모델로 재현, 전부 한 번만 반영됨.
8. **최근 본 상품 · 연속 판정 엣지** (깨진 시각·수동 기록·경기중) 전부 정상.

시뮬레이션 중 고친 것: 방 헤더의 position 클래스 충돌(sticky+relative → 헤더 고정이
깨질 수 있던 잠재 버그) 제거, 로그인 휴대폰 번호 자동 하이픈(010-1234-5678) 추가.

## 이번에 들어가지 않은 것 (요청 항목 중)

- **[45] 서버 푸시(앱 완전 종료 상태 알림)** — Cloud Functions(Blaze 요금제) 필요.
  절차는 docs/OPS.md 2번. 클라이언트 쪽 알림 3종은 이번에 들어감.
- **[99] 예산 알림 실제 설정** — Firebase/GCP 콘솔에서만 가능. 절차는 docs/OPS.md 1번.
